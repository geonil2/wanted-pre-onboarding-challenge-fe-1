import React, {useEffect} from 'react';

const Home = () => {
  useEffect(() => {
    console.log('home~~')
    }, [])

    return (
        <div>
            home
        </div>
    );
};

export default Home;
